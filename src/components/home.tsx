import * as React from "react";

const Home = () => {
  return (
    <div className="h-full py-2 bg-white shadow shadow-slate-800">
      {/*TODO: READ STATE FROM REDUX AND ADD INFORMATION REQUIRED HERE */}
      Welcome page hello this should display some information and probably
      remove the sidebar.
    </div>
  );
};

export default Home;
